{
  "name": "taps-capture",
  "version": "1.0.0",
  "description": "Capture text and audio to Obsidian Daily Notes with tags",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "devDependencies": {
    "@types/node": "^16.11.6",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.25.0",
    "obsidian": "latest",
    "tslib": "2.4.0",
    "typescript": "^5.8.0"
  }
}
